# TODO

- [ ] Make it so that the transform doesn't automatically crawl the whole
      assembly. This can be slow in larger codebases.
- [ ] Add support for generation of grammar json files for VS Code highlighting.
- [ ] Add support for lexon specification in the grammar file.
